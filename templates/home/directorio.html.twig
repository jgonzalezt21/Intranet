{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css"
          href="{{ asset('static/vendor/dataTables/dataTables/css/dataTables.bootstrap5.min.css') }}"/>
{% endblock %}

{% block body %}
    <div class="container main py-4">
        <h4><i class="bi bi-telephone"></i> {{ controller_name }}:</h4>

        <div class="table-responsive mt-4">
            <table class="table table-striped table-hover " id="datos_directorio">
                <thead>
                <tr>
                    <th scope="col">Lugar</th>
                    <th scope="col">Número</th>
                </tr>
                </thead>
                <tbody>
                {% for dat in data %}
                    <tr>
                        {% for d in dat %}
                            <td>{{ d }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('static/vendor/jquery.min.js') }}"></script>
    <script type="text/javascript"
            src="{{ asset('static/vendor/dataTables/dataTables/js/jquery.dataTables.min.js') }}"></script>
    <script type="text/javascript"
            src="{{ asset('static/vendor/dataTables/dataTables/js/dataTables.bootstrap5.min.js') }}"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#datos_directorio').DataTable({
                "language": {
                    "url": "{{ asset('static/vendor/dataTables/es.json') }}"
                }
            });
        });
    </script>
{% endblock %}
